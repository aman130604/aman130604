name: Snake Animation

on:
  schedule:
    - cron: "0 0 * * *"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: Platane/snk@v3
        with:
          github_user_name: aman130604
          outputs: dist/github-contribution-grid-snake.svg
